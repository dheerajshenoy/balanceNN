#pragma once

// Precompiled header. Keep this list short and stable — anything included
// here is baked into every TU that uses the PCH, so header churn will
// invalidate the whole cache.

#include <torch/torch.h>
